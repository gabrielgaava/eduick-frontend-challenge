import styled from 'styled-components';
import colors from '../../styles/colors';
import { Page } from '../../styles/basicStyles';

export const PageBackground = styled(Page)`
  background: ${colors.primaryGradiant};
`